TODO(aryan): drop the resume PDF here, named exactly:

    Aryan-Tiwari-Resume.pdf

That filename is referenced by `profile.resumePath` in constants/profile.ts and
is used by the hero "Download Resume" button and the resume section. Until the
file exists, those links will 404.
