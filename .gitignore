node_modules
.next
out
build
*.log
.DS_Store
.vercel
next-env.d.ts
*.tsbuildinfo

# Secrets stay local. `.env.example` is committed on purpose.
# `.env*` covers .env.local, .env.production, .env.test — all of which can
# legitimately hold real keys in a Next.js project.
.env*
!.env.example
